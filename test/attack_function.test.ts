import { is_army_empty } from '../attack_functions'
import { type Army } from '../types'

//Mall för tests
describe('funktionsnamn', () => {
    it(' ', () => {
        
        }

        expect().toEqual();
    });


it ('is_army_empty', () => {
    const empty_army : Army =
    is_army_empty
})
