//import { MatrixGraph } from "../lib/graphs";
//import { finds_paths } from "../Functions/general_functions";

//import { create_army, create_castle } from "../Functions/setup_functions";

//import { Army, Castle, Warrior } from "../types";

/*describe('', () => {
    it('test 1: ', () => {
        
        }

        expect().toEqual();
    });

*/
describe('', () => {
    
    it('test 1: ', () => {
        
        /*
        let army = create_army();
        const castle = create_castle(army,"TestOwner",1);

        let I = true;
        let O = false;
        const matrix_map = {
            size: 3,
            adj: 
                [
                    [O,I,O],
                    [O,O,I],
                    [O,O,O]
                ]
        }
        //const arr = finds_paths(castle, matrix_map);
        
        
        */
        expect(true).toBe(true);
    
        });

    });